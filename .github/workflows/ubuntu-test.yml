name: Test on ubuntu:latest

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2

      - name: Install packages
        run: sudo apt-get install -y git stow
        
      - name: Run setup.sh
        run: bash setup.sh
        
  curl:
    runs-on: ubuntu-latest
    steps:
      - name: Install packages
        run: sudo apt-get install -y git stow
        
      - name: Run curl
        run: curl -L ${{ github.server_url }}/${{ github.repository }}/blob/${{ github.ref_name }}/remote.sh | bash
        env:
          BRANCH: ${{ github.ref_name }}
