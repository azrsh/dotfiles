name: Test on ubuntu:latest

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2

      - name: Install packages
        run: sudo apt-get install -y make stow
        
      - name: Run make
        run: make
